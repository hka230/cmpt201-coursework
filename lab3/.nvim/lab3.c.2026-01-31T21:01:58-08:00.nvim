#include <stdio.h>
#include <stdlib.h>

#define _POSIX_C_SOURCE 200809L

int main() {

  char* queue[5];

  char* buffer = NULL;
  size_t size = 0;

  printf("Enter Input:");

  while(getline(&buffer,&size,stdin) != -1) {

    printf("%f",buffer);

  }

  return 0;

}
