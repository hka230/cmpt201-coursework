#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main() {
  
  int indexer = 0;

  char *queue[5];

  char *buffer = NULL;
  size_t size = 0;

  printf("Enter Input:");

  while (getline(&buffer, &size, stdin) != -1) {
    
    if(strcmp(buffer, "print\n")) {
      printf("print mentioned");
    }
    else {

    }

    printf("\nEnter Input:");   
  }

  return 0;
}
